# E-Mail-Log Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jede vom Admin verschickte E-Mail (Einladung, Erinnerung, Absage, Update) wird in PocketBase geloggt und in einem neuen Tab in admin.html angezeigt.

**Architecture:** Der PocketBase-Hook schreibt nach jedem Resend-Call einen Record in eine neue `email_log` Collection. Das Frontend liest diese Records per `pbList` und rendert sie in einem neuen Tab in admin.html.

**Tech Stack:** Vanilla JS, PocketBase v0.38 Goja-Hooks, Resend HTTP API, keine Build-Tools

---

## Dateien

| Aktion | Datei |
|---|---|
| Modify | `.pb_hooks/main.pb.js` — sendMail um email_log-Write erweitern |
| Create | `js/emailLog.js` — `renderEmailLog()` Funktion |
| Modify | `admin.html` — neuer Tab + Script-Tag |

---

### Task 1: PocketBase Collection `email_log` manuell anlegen

**Dateien:** keine (manueller Schritt im PB Admin UI)

- [ ] **Schritt 1: PB Admin UI öffnen**

  Öffne `https://api.crewplanner.nyxlightwork.de/_/` und logge dich ein.

- [ ] **Schritt 2: Collection importieren**

  Settings → Import collections → folgendes JSON einfügen:

  ```json
  [{"name":"email_log","type":"base","listRule":"@request.auth.id != \"\"","viewRule":"@request.auth.id != \"\"","createRule":"@request.auth.id != \"\"","updateRule":"","deleteRule":"@request.auth.role = \"manager\" || @request.auth.role = \"superadmin\"","fields":[{"name":"plan_id","type":"text","required":true},{"name":"crew_name","type":"text","required":true},{"name":"crew_email","type":"email","required":true},{"name":"email_type","type":"text","required":true},{"name":"sent_at","type":"date","required":true},{"name":"success","type":"bool","required":true}]}]
  ```

  → "Merge with existing collections" anhaken → "Review" → "Confirm"

- [ ] **Schritt 3: Collection verifizieren**

  PB Admin → Collections → `email_log` sichtbar mit 6 Feldern: `plan_id`, `crew_name`, `crew_email`, `email_type`, `sent_at`, `success`.

---

### Task 2: Hook `main.pb.js` erweitern

**Dateien:**
- Modify: `.pb_hooks/main.pb.js` — in der `sendMail`-Funktion (ab ca. Zeile 22)

- [ ] **Schritt 1: sendMail-Funktion anpassen**

  Aktuelle `sendMail`-Funktion in `.pb_hooks/main.pb.js` (Zeilen 22–34):

  ```js
  var sendMail = function(to, subject, html) {
    try {
      var _key  = $os.getenv('RESEND_KEY');
      var _from = 'Tour Crew Plan <noreply@crewplanner.nyxlightwork.de>';
      var res = $http.send({
        url: 'https://api.resend.com/emails', method: 'POST',
        headers: { 'Authorization': 'Bearer ' + _key, 'Content-Type': 'application/json' },
        body: JSON.stringify({ from: _from, to: [to], subject: subject, html: html })
      });
      if (res.statusCode >= 400) { console.error('[mail] Resend Fehler ' + res.statusCode + ':', res.raw); }
      else { console.log('[mail] Gesendet an ' + to + ' · ' + subject); }
    } catch (err) { console.error('[mail] Fehler:', err.message || String(err)); }
  };
  ```

  Ersetzen durch:

  ```js
  var sendMail = function(to, subject, html) {
    try {
      var _key  = $os.getenv('RESEND_KEY');
      var _from = 'Tour Crew Plan <noreply@crewplanner.nyxlightwork.de>';
      var res = $http.send({
        url: 'https://api.resend.com/emails', method: 'POST',
        headers: { 'Authorization': 'Bearer ' + _key, 'Content-Type': 'application/json' },
        body: JSON.stringify({ from: _from, to: [to], subject: subject, html: html })
      });
      var _success = res.statusCode < 400;
      if (!_success) { console.error('[mail] Resend Fehler ' + res.statusCode + ':', res.raw); }
      else { console.log('[mail] Gesendet an ' + to + ' · ' + subject); }
      try {
        var _logCol = $app.findCollectionByNameOrId('email_log');
        var _logRec = new Record(_logCol);
        _logRec.set('plan_id',    r.get('plan_id') || '');
        _logRec.set('crew_name',  name);
        _logRec.set('crew_email', email);
        _logRec.set('email_type', type);
        _logRec.set('sent_at',    new Date().toISOString());
        _logRec.set('success',    _success);
        $app.save(_logRec);
        console.log('[mail] email_log gespeichert · ' + type);
      } catch (logErr) { console.error('[mail] email_log Fehler:', logErr.message || String(logErr)); }
    } catch (err) { console.error('[mail] Fehler:', err.message || String(err)); }
  };
  ```

- [ ] **Schritt 2: Hook-Version auf 4.1 erhöhen**

  Zeile 1 in `.pb_hooks/main.pb.js`:
  ```js
  // Version: 4.0
  ```
  ändern zu:
  ```js
  // Version: 4.1
  ```

  Zeile 4:
  ```js
  console.log('[hook] main.pb.js v4.0 geladen');
  ```
  ändern zu:
  ```js
  console.log('[hook] main.pb.js v4.1 geladen');
  ```

- [ ] **Schritt 3: Hook deployen**

  ```bash
  ssh «SERVER» "curl -s -o /var/lib/docker/volumes/«PB-HOOKS-VOLUME-LIVE»/_data/main.pb.js \
    https://raw.githubusercontent.com/Aniflu/Crewplaner/main/.pb_hooks/main.pb.js \
    && docker restart «PB-CONTAINER-LIVE»"
  ```

  Erwartete Ausgabe: Container-ID, dann kurze Pause.

- [ ] **Schritt 4: Hook-Start verifizieren**

  ```bash
  ssh «SERVER» "docker logs «PB-CONTAINER-LIVE» --tail 10"
  ```

  Erwartete Ausgabe enthält: `[hook] main.pb.js v4.1 geladen`

- [ ] **Schritt 5: Commit**

  ```bash
  git add .pb_hooks/main.pb.js
  git commit -m "feat: hook schreibt email_log nach jedem mailversand (v4.1)"
  ```

---

### Task 3: `js/emailLog.js` erstellen

**Dateien:**
- Create: `js/emailLog.js`

- [ ] **Schritt 1: Datei erstellen**

  Neue Datei `js/emailLog.js` mit folgendem Inhalt:

  ```js
  // ── E-Mail-Log Tab (nur Admin) ───────────────────────────────────────────────
  const EMAIL_TYPE_LABELS = {
    invite:       '📧 Einladung',
    reminder:     '🔔 Erinnerung',
    cancellation: '✕ Absage',
    update:       '↻ Update',
    love_invite:  '♥ Partner-Einladung',
    staff_invite: '👤 Staff-Einladung',
  };

  async function renderEmailLog() {
    const container = document.getElementById('emailLogBody');
    if (!container) return;
    if (!_wrkPbPlanId) {
      container.innerHTML = '<p style="font-size:.65rem;color:#5a6070;">Bitte zuerst einen Plan auswählen.</p>';
      return;
    }
    container.innerHTML = '<p style="font-size:.65rem;color:#5a6070;">Lädt…</p>';
    try {
      const records = await pbList('email_log', {
        filter: 'plan_id="' + _wrkPbPlanId + '"',
        sort: '-sent_at',
        perPage: 200,
      });
      if (!records.length) {
        container.innerHTML = '<p style="font-size:.65rem;color:#5a6070;">Noch keine E-Mails für diesen Plan.</p>';
        return;
      }
      const rows = records.map(function(rec) {
        const d = new Date(rec.sent_at);
        const dateStr = d.getDate().toString().padStart(2,'0') + '.' +
          (d.getMonth()+1).toString().padStart(2,'0') + '. ' +
          d.getHours().toString().padStart(2,'0') + ':' +
          d.getMinutes().toString().padStart(2,'0');
        const label = EMAIL_TYPE_LABELS[rec.email_type] || rec.email_type;
        const status = rec.success
          ? '<span style="color:#4ae8a0;">✅</span>'
          : '<span style="color:#e84a4a;">❌</span>';
        return '<tr>' +
          '<td style="color:#c8cdd5;">' + dateStr + '</td>' +
          '<td style="color:#c8cdd5;">' + esc(rec.crew_name) + '</td>' +
          '<td style="color:#c8cdd5;">' + label + '</td>' +
          '<td>' + status + '</td>' +
          '</tr>';
      }).join('');
      container.innerHTML =
        '<table class="data-table" style="width:100%;">' +
        '<thead><tr>' +
        '<th>Datum</th><th>Crew</th><th>Typ</th><th>Status</th>' +
        '</tr></thead>' +
        '<tbody>' + rows + '</tbody>' +
        '</table>';
    } catch (err) {
      container.innerHTML = '<p style="font-size:.65rem;color:#e84a4a;">Fehler: ' + esc(err.message || String(err)) + '</p>';
    }
  }
  ```

  Hinweis: `esc()` und `pbList()` sind globale Funktionen aus `utils.js` bzw. `pb.js` — immer verfügbar.

- [ ] **Schritt 2: Commit**

  ```bash
  git add js/emailLog.js
  git commit -m "feat: emailLog.js mit renderEmailLog()"
  ```

---

### Task 4: `admin.html` — Tab + Script einbinden

**Dateien:**
- Modify: `admin.html`

- [ ] **Schritt 1: Script-Tag hinzufügen**

  In `admin.html`, nach dem letzten `<script src=...>` am Anfang (aktuell Zeile 10: `js/pdf.js?v=27`), neues Script einfügen:

  ```html
  <script src="js/emailLog.js?v=1"></script>
  ```

- [ ] **Schritt 2: Tab-Button hinzufügen**

  In `admin.html`, nach Zeile 457 (nach dem `Pläne`-Tab-Button):

  ```html
  <button class="nav-tab" data-tab="emaillog" onclick="switchTab('emaillog'); renderEmailLog()">E-Mail-Log</button>
  ```

- [ ] **Schritt 3: Tab-Section hinzufügen**

  In `admin.html`, nach dem schließenden `</div>` von `tabPlans` (suche nach `id="tabPlans"`), neuen Abschnitt einfügen:

  ```html
  <!-- E-MAIL-LOG -->
  <div class="section" id="tabEmaillog">
    <div class="section-title">E-Mail-Log</div>
    <div class="section-sub">Alle für diesen Plan versendeten E-Mails</div>
    <div id="emailLogBody" style="margin-top:16px;">
      <p style="font-size:.65rem;color:#5a6070;">Tab anklicken um zu laden.</p>
    </div>
  </div>
  ```

- [ ] **Schritt 4: Manuell testen**

  1. `python3 -m http.server 8080` starten, `http://localhost:8080/admin.html` öffnen
  2. Einloggen → Tab "E-Mail-Log" erscheint in der Nav
  3. Plan auswählen → Tab "E-Mail-Log" anklicken
  4. Tabelle lädt (leer wenn noch keine Mails gesendet)
  5. Im "Crew benachrichtigen" Modal eine Test-Einladung senden
  6. Tab "E-Mail-Log" neu klicken → neuer Eintrag erscheint mit Datum, Name, "📧 Einladung", ✅

- [ ] **Schritt 5: Commit**

  ```bash
  git add admin.html
  git commit -m "feat: e-mail-log tab in admin konsole"
  ```

---

### Task 5: Version erhöhen + deployen

**Dateien:**
- Modify: `index.html`, `admin.html`, `CLAUDE.md`, `README.md`

- [ ] **Schritt 1: User nach Versionsnummer fragen**

  Vorschlag: `v0.9.9.8` (kleines Feature-Increment). User entscheidet.

- [ ] **Schritt 2: Version in 4 Dateien setzen**

  - `index.html`: `<span class="tour-tag">Personalplan · vX.X.X</span>`
  - `admin.html`: `<span style="...">vX.X.X</span>` im Header
  - `CLAUDE.md`: "Aktuelle Version: **vX.X.X**"
  - `README.md`: Version-Zeile

- [ ] **Schritt 3: Push to Production**

  ```bash
  git add index.html admin.html CLAUDE.md README.md
  git commit -m "chore: version bump vX.X.X"
  git push origin main
  ```

  GitHub Pages aktualisiert sich automatisch ~1 Minute danach.
